/*
 * This is a demostration on how small we can achieve for a C program on RISC-V system.
 *
 * So far this results in 896 bytes in binary size, and 330 bytes in gzip format
 */
int main()
{
  return 0;
}
